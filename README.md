# Portfolio Data Branch

This branch stores data generated by the portfolio website backend.

## Structure

```
data/
  contacts/     # Contact form submissions (JSON files)
```

Each contact submission is saved as a timestamped JSON file with the sender's name, email, message, and timestamp.
